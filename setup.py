from distutils.core import setup
setup(
  name = 'pyzipcode-cli',
  packages = ['pyzipcode-cli'], # this must be the same as the name above
  version = '0.0.1',
  description = "pyzipcode-cli is a thin wrapper around getziptastic's API v2",
  author = 'Tasdik Rahman',
  author_email = 'tasdik95@gmail.com',
  url = 'https://github.com/prodicus/pyzipcode-cli', # use the URL to the github repo
  download_url = 'https://github.com/peterldowns/mypackage/tarball/0.1', # I'll explain this in a second
  keywords = ['testing', 'geo-location', 'zipcode'], # arbitrary keywords
  classifiers = [],
)